# DECISIONS

# 2026-06-19 현 버전에서는 3D 런타임을 의존성에서 제거

## 결정

현재 검색 중심 버전에서는 React Three Fiber, Drei, Three.js 런타임 의존성을 제거한다.

## 이유

- 사용자는 3D를 추후 추가할 기능으로 정리했고, 현재 팀원용 핵심은 장비 검색, 필터, 상세, 출처, 결과 공유이다.
- 실제 GLB 모델이 아직 준비되지 않았으므로 3D 런타임을 유지해도 현재 사용자 가치가 없다.
- 미사용 의존성과 구형 컴포넌트를 제거하면 설치 크기와 유지보수 표면이 줄어든다.

## 영향

- 현재 화면은 계속 GLB 준비 슬롯과 부품 스펙 목록을 보여준다.
- 향후 실제 3D 뷰어를 복구할 때는 별도 결정으로 `three`, `@react-three/fiber`, `@react-three/drei`를 다시 추가한다.
- `hotspotPosition` 데이터 필드는 추후 3D 핫스팟 재사용 가능성을 위해 유지한다.

---

# 2026-06-19 검색 결과 내보내기는 클라이언트에서 처리

## 결정

필터된 장비 결과의 요약 복사와 CSV 다운로드는 서버 없이 브라우저 클라이언트에서 생성한다.

## 이유

- 현재 앱은 정적 GitHub Pages 배포 구조이므로 서버 저장, 파일 생성 API, 로그인 기능을 두지 않는다.
- 팀원이 회의록이나 메신저에 결과를 붙여 넣거나 스프레드시트로 열기에는 클라이언트 생성 CSV가 충분하다.
- 필터 결과와 URL 공유 기능을 조합하면 같은 조건과 결과를 함께 전달할 수 있다.

## 영향

- CSV는 현재 화면의 필터 결과 기준으로 생성된다.
- CSV 컬럼은 장비 검색과 후속 정리에 필요한 최소 필드로 제한한다.
- Clipboard API가 제한되는 환경에서도 CSV 다운로드는 계속 사용할 수 있다.

---

# 2026-06-19 검색 조건은 URL query string으로 공유

## 결정

장비 검색 조건은 서버 저장이나 계정 기능 없이 URL query string으로 공유한다.

## 이유

- 현재 앱은 정적 GitHub Pages 배포 구조라 서버 저장소나 로그인 기능이 없다.
- 팀원이 메신저, 문서, 회의 자료에서 같은 검색 결과를 열려면 URL 기반 복원이 가장 단순하고 안정적이다.
- 필터 상태가 브라우저 URL에 남으면 새로고침, 뒤로가기, 링크 전달 동작을 자연스럽게 지원할 수 있다.

## 영향

- 검색 화면은 `q`, `family`, `category`, `role`, `country`, `status`, `maturity`, `eq` 파라미터를 읽고 쓴다.
- 상세/출처/인사이트 화면은 기존 라우팅을 유지하고, 검색 화면에서만 query string 동기화를 수행한다.
- Playwright 검증은 필터 적용 후 URL 파라미터 생성과 reload 후 결과 복원을 확인한다.

---

# 2026-06-19 검색 필터는 기존 데이터에서 파생

## 결정

장비 검색 고도화는 새 JSON 스키마를 먼저 늘리지 않고, 기존 `equipment`와 `variants` 데이터에서 필터 옵션을 자동 파생한다.

## 이유

- 현재 목표는 팀원이 필요한 장비를 빠르게 찾는 것이며, 데이터 구조 개편보다 검색 경험 개선이 우선이다.
- `roleTags`, `operatorCountries`, `status`, `variants.role`, `variants.maturity`만으로도 임무/국가/상태/계열 성숙도 검색을 구현할 수 있다.
- 전 세계 장비 DB 확장 전에는 스키마를 과도하게 늘리기보다 실제 사용 흐름을 먼저 검증하는 편이 유지보수에 유리하다.

## 영향

- 검색 패널에 임무/파생형, 국가, 운용 상태, 계열 성숙도 필터가 추가됐다.
- 데이터 추가 시 기존 필드를 채우면 필터 옵션이 자동으로 늘어난다.
- 향후 대량 장비 DB를 넣을 때 필요성이 확인되면 `generation`, `driveType`, `serviceCountries` 같은 명시 필드를 별도 결정으로 추가한다.

---

## 2026-06-19 Harness-based Development 문서 체계 도입

### 결정

프로젝트 루트에 `AGENTS.md`, `PROJECT_STATE.md`, `TASKS.md`, `DECISIONS.md`, `EVIDENCE.md`, `CHANGELOG.md`를 운영 문서 세트로 유지한다.

### 이유

- 기능 구현보다 반복 가능한 개발 루프와 검증 증거를 우선하기 위해서다.
- Codex가 매 작업 시작 시 현재 상태, 결정, 검증 증거를 확인할 수 있게 하기 위해서다.
- 단발성 코드 생성이 아니라 장기 유지보수 가능한 프로젝트로 운영하기 위해서다.

### 영향

- 신규 작업은 Clarify → Plan → Execute → Verify → Evidence → Decide 순서를 따른다.
- 아키텍처 변경, 라이브러리 교체, 대규모 리팩토링, 데이터 구조 변경, 기존 기능 제거는 사용자 승인 후 진행한다.
- 완료 선언 전 검증 명령과 증거 기록을 남긴다.

## 2026-06-19 문서 전용 변경의 검증 범위

### 결정

이번 작업은 앱 코드 변경이 아니므로 전체 `npm run quality` 대신 문서 존재 확인, Git 상태 확인, 가능한 경량 검증을 우선한다.

### 이유

- 문서 생성 작업에서 전체 Playwright/빌드 검증은 비용이 높다.
- 단, 운영 원칙상 증거가 필요하므로 명령 실행 결과는 `EVIDENCE.md`에 남긴다.

### 영향

- 기능 코드 변경 작업에서는 기존 원칙대로 build/test/lint 우선순위를 적용한다.
- 문서 전용 작업에서는 문서 존재성 및 Git 상태를 필수 증거로 남긴다.
# 2026-06-19 장비 검색 카탈로그를 제품 중심축으로 채택

## 결정

웹페이지의 1차 사용 목적을 회의 브리핑이 아니라 `전 세계 지상 장비 검색 카탈로그`로 둔다.

## 이유

- 사용자가 팀원에게 필요한 핵심 목적을 장비 검색이라고 명확히 했다.
- 장비 데이터는 계속 확장될 예정이므로, 첫 화면에서 검색/분류/상세 진입이 바로 가능해야 한다.
- 기존 브리핑형 대시보드, 비교 페이지, 기술/사례 분리 메뉴는 검색 목적에서는 초점을 분산시킨다.

## 영향

- 상단 메뉴를 `장비 검색 / 전체 장비 / 전장 인사이트 / 출처`로 축소했다.
- `Compare`는 별도 메뉴에서 제외하고, 상세 화면의 유사 장비 추천으로 대체했다.
- `Technologies`, `Cases`, `Development`는 `전장 인사이트` 화면으로 통합했다.
- 기존 데이터는 삭제하지 않고 검색/상세/인사이트/출처 화면에서 재사용한다.

# 2026-06-19 3D 기능 단순화

## 결정

현재 버전에서 3D 캔버스를 기본 표시하지 않고, GLB 추후 연동 슬롯과 부품 스펙 목록으로 단순화한다.

## 이유

- 실제 GLB 모델이 아직 준비되지 않았고, 사용자는 3D를 추후 추가하겠다고 밝혔다.
- 플레이스홀더 3D는 팀원의 장비 검색 목적에서는 우선순위가 낮다.
- 3D 런타임을 기본 렌더링에서 제외하면 번들 크기와 화면 복잡도가 줄어든다.

## 영향

- `ModelViewer`는 캔버스 대신 준비 슬롯과 부품 스펙 버튼을 렌더링한다.
- Playwright 검증은 캔버스 대신 `.model-slot`과 `.component-slot`을 확인한다.

---
# 2026-06-19 3D 준비 영역을 공개 장치 스펙 패널로 전환

## 결정

현재 버전에서 3D/GLB 준비 영역을 화면에 표시하지 않고, 장비별 공개 장치/부품 스펙 패널을 기본 상세 보조 패널로 사용한다.

## 이유

- 사용자의 현재 목적은 장비 검색용 카탈로그이며, 3D는 추후 추가 대상이다.
- 실제 GLB 모델이 없는 상태에서 3D 준비 문구를 계속 노출하면 팀원이 현재 사용 가능한 기능으로 오해할 수 있다.
- 이미 수집된 `components.json` 데이터는 장비 검색, 스펙 확인, 출처 검토에 바로 가치가 있다.

## 영향

- UI는 장비 검색과 공개 스펙 확인에 더 집중된다.
- 3D 재도입 시에는 별도 결정으로 `modelPath`, `hotspotPosition`, 3D 의존성을 다시 연결한다.
- 검증 스크립트는 3D 슬롯 존재가 아니라 장치 스펙 패널 존재를 기준으로 한다.

---
# 2026-06-19 검색 결과 단계에서 출처 신뢰도를 노출

## 결정

장비 검색 결과 행과 선택 장비 요약 패널에 출처 신뢰도, 최근 확인일, 공개 출처 수를 직접 표시한다.

## 이유

- 팀원이 장비를 검색한 뒤 바로 회의나 보고서에 활용하려면 데이터의 근거 수준을 상세 페이지로 들어가기 전에 판단할 수 있어야 한다.
- 이미 `sourceConfidenceScore`, `sources.checkedAt`, `sources.length` 데이터가 있으므로 스키마 변경 없이 구현할 수 있다.
- CSV와 요약 복사에도 같은 정보를 포함하면 공유된 검색 결과만 보아도 검증 맥락이 유지된다.

## 영향

- 검색 행의 정보량은 늘어나지만, 배지와 보조 라인으로 분리해 모바일 overflow 없이 표시한다.
- 출처 신뢰도는 자동 검증 결과가 아니라 현재 데이터셋의 공개 출처 기반 참고 지표임을 유지한다.
- 향후 출처 정기 재검증을 자동화하면 같은 표시 영역에 최신 검증일을 계속 반영할 수 있다.

---
