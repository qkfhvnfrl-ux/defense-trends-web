# 2026-06-20 상태 갱신: 적용 필터 배지 표시

- 장비 검색 패널의 적용 조건 영역에 실제 필터명을 배지로 표시하도록 개선했다.
- 기존 `N개 조건 적용`만으로는 현재 목록의 의미를 바로 알기 어려워, 계열/분류/임무/국가/신뢰도/사례/데이터 상태/정렬/검색어를 사람이 읽는 라벨로 노출한다.
- 프리셋이나 공유 링크로 진입했을 때도 적용된 조건을 즉시 확인할 수 있다.
- 검증 기준에 프리셋 적용 후 필터 배지 1개 이상 표시와 배지 컨트롤 크기 검사를 추가했다.
- 검증 결과 `npm run quality`에서 프리셋 적용 후 필터 배지 1개와 브라우저 오류 없음이 확인됐다.

# 2026-06-20 상태 갱신: 선택 장비 요약 복사

- 선택 장비 요약 패널에 `요약 복사` 작업을 추가했다.
- 복사 내용은 장비명, 분류, 국가/원산국, 제조사, 운용 상태, 핵심 임무, 계열/전장 사례/연계 기술 수, 출처 신뢰도, 데이터 상태, 최근 확인일, 요약문으로 구성했다.
- 기존 결과 전체 내보내기와 별개로, 팀원이 메신저나 회의록에 단일 장비 브리프를 바로 붙일 수 있게 했다.
- 검증 기준에 선택 장비 빠른 작업 버튼 2개와 컨트롤 크기 검사를 추가했다.
- 검증 결과 `npm run quality`에서 선택 장비 작업 버튼 2개와 브라우저 오류 없음이 확인됐다.

# 2026-06-20 상태 갱신: 중복 상단 메뉴 축소

- 상단 메뉴에서 `전체 장비` 항목을 제거해 첫 화면 노출을 `장비 검색`, `전장 인사이트`, `출처` 3개로 단순화했다.
- `/equipment`와 `/equipment/:id` 라우트는 기존 공유 링크와 상세 페이지 호환을 위해 유지한다.
- 장비 상세 또는 `/equipment` 경로에서도 `장비 검색` 메뉴가 활성화되도록 정리했다.
- 검증 기준에 상단 메뉴 3개와 `전체 장비` 버튼 미노출 조건을 추가했다.
- 검증 결과 `npm run quality`에서 상단 메뉴 3개, `전체 장비` 버튼 미노출, 기존 `/equipment` 라우트 렌더링이 확인됐다.

# 2026-06-20 상태 갱신: 팀 작업 큐 추가

- 첫 화면 개요 영역에 팀원이 바로 판단할 수 있는 작업 큐 4개를 추가했다.
- 큐 항목은 `보강 필요 장비`, `출처 재확인`, `실전 사례 장비`, `계열 비교 후보`로 구성했다.
- 장비 큐는 기존 검색 프리셋과 연결해 클릭 즉시 필터/정렬/URL 동기화가 적용된다.
- 출처 재확인 큐는 `/sources?freshness=stale`로 이동하며, 현재 데이터 기준 재확인 필요 출처가 0건일 때도 빈 상태 안내가 표시된다.
- 검증 결과 작업 큐 버튼 4개, 보강 필요 결과 3건, 출처 재확인 빈 상태 1개, 모바일 가로 넘침 없음이 확인됐다.

# 2026-06-19 상태 갱신: 팀용 장비 검색 프리셋

- 장비 검색 패널에 반복 업무용 프리셋 4개를 추가했다.
- 프리셋은 `보강 필요`, `실전 사례`, `고신뢰 출처`, `계열 많은 장비`로 구성했다.
- 3D나 상세 분석 화면을 추가하지 않고, 현재 목표인 전 세계 장비 검색 흐름을 빠르게 좁히는 기능으로 제한했다.
- 첫 프리셋은 `data=needs-review` URL 상태와 동기화되어 팀원에게 같은 검색 조건을 공유할 수 있다.
- 검증 결과 프리셋 버튼 4개, 보강 필요 결과 3건, 장비 목록 15건, 모바일 가로 넘침 없음이 확인되었다.

# 2026-06-19 상태 갱신: 출처 필터 결과 내보내기

- `/sources` 화면에 필터링된 출처 결과 기준 `출처 요약 복사`와 `출처 CSV 다운로드`를 추가했다.
- CSV 파일명은 `source-index-results.csv`로 고정해 반복 공유/저장이 쉽도록 했다.
- 출처 검색, 유형 필터, 확인 상태 필터를 적용한 현재 결과만 복사/다운로드한다.
- 검증 결과 출처 카드 46건, 필터 결과 CSV 다운로드 파일명, 내보내기 버튼 2개가 확인됐다.

# 2026-06-19 상태 갱신: 출처 인덱스 필터/확인 상태

- `/sources` 화면에 출처 검색, 출처 유형 필터, 확인 상태 필터를 추가했다.
- 출처 확인 상태는 `checkedAt` 기준 180일 초과 시 `재확인 필요`, 그 외 `최근 확인`으로 표시한다.
- 출처 인덱스 상단에 최근 확인, 재확인 필요, 현재 표시 건수를 요약한다.
- 현재 정적 데이터 기준 출처 카드 46건, 공식 출처 16건, Rheinmetall 검색 결과 5건이 검증됐다.
- 실시간 URL 접속 검증은 기존 `npm run sources:check` 스크립트로 분리 유지한다.

# 2026-06-19 상태 갱신: 데이터 보강 필요 필터

- 장비 검색 필터에 `데이터 상태`를 추가했다.
- `검증 양호`와 `보강 필요`로 장비를 분류하며, 보강 필요 기준은 출처 신뢰도 68 미만, 공개 출처 2건 미만, 계열 데이터 없음 중 하나 이상이다.
- 검색 결과 행에 데이터 상태 배지를 추가해 목록 단계에서 보강 대상을 즉시 식별할 수 있다.
- 공유 URL에는 `data=ready` 또는 `data=needs-review` 파라미터가 반영된다.
- 현재 데이터 기준 보강 필요 장비는 3건으로 검증됐다.

# 2026-06-19 상태 갱신: 검색 결과 핵심 지표 스트립

- 장비 검색 결과 행에 `운용국`, `계열`, `전장 사례`, `출처` 핵심 지표를 추가했다.
- 팀원이 상세 화면에 들어가기 전에 목록에서 장비의 운용 확산도, 파생형 규모, 실전 사례 보유 여부, 공개 출처 규모를 즉시 비교할 수 있다.
- 데이터 구조 변경 없이 기존 `operatorCountries`, `variants`, `battlefieldCaseIds`, `sources` 값을 사용한다.
- 렌더/디자인 검증은 장비 15건 각각에 지표 스트립 1개와 지표 셀 4개가 있는지 확인한다.
- 다음 우선순위는 검색 결과의 `결측/보강 필요` 상태 표시와 데이터 확장 작업이다.

# PROJECT_STATE

## 2026-06-19 상태 갱신: 불필요한 3D/구형 코드 제거

- 현재 검색 중심 버전에서 사용하지 않는 3D 런타임 의존성을 제거했다.
- 제거한 패키지는 `@react-three/drei`, `@react-three/fiber`, `three`, `pngjs`이다.
- 사용되지 않는 `DevelopmentLensPage`, `TrendPanel` 컴포넌트를 삭제했다.
- 3D 캔버스 조작 UI와 구형 개발 렌즈 페이지 전용 CSS를 제거했다.
- `hotspotPosition` 데이터 필드는 추후 GLB 핫스팟 복구를 위해 유지한다.

## 2026-06-19 상태 갱신: 검색 결과 내보내기

- 필터된 장비 목록을 팀원에게 전달할 수 있도록 결과 내보내기 기능을 추가했다.
- 검색 패널에서 `결과 요약 복사`와 `CSV 다운로드`를 제공한다.
- CSV는 현재 필터 결과 기준으로 장비명, 분류, 국가, 원산국, 제조사, 운용 상태, 임무 태그, 계열 수, 출처 신뢰도, 상세 ID를 포함한다.
- CSV 파일명은 `equipment-search-results.csv`로 고정해 반복 공유와 저장이 쉽도록 했다.

## 2026-06-19 상태 갱신: 검색 조건 공유 링크

- 장비 검색 화면의 필터 상태가 URL query string에 동기화된다.
- 공유 가능한 검색 조건은 `q`, `family`, `category`, `role`, `country`, `status`, `maturity`, `eq` 파라미터로 표현한다.
- 검색 링크를 새로고침하거나 팀원에게 전달해도 같은 필터 결과가 복원된다.
- 검색 패널에 `검색 링크 복사` 버튼을 추가했다.

## 2026-06-19 상태 갱신: 팀원용 검색 필터 고도화

- 장비 검색 화면에 세부 필터를 추가했다.
- 필터 축은 `차륜/궤도 계열`, `장비 분류`, `임무/파생형`, `국가`, `운용 상태`, `계열 성숙도`, `자유 검색어`로 구성한다.
- 기존 JSON 스키마는 유지하고 `roleTags`, `operatorCountries`, `status`, `variants.role`, `variants.maturity`에서 필터 옵션을 자동 파생한다.
- 검색 결과 행에 역할 태그와 계열 수를 표시해 팀원이 상세 페이지로 들어가기 전 기본 판단을 할 수 있게 했다.
- 3D는 계속 추후 기능으로 유지하고 현재 화면은 검색/상세/출처 중심이다.

## 2026-06-19 상태 갱신

- 현재 제품 방향을 `전 세계 지상 장비 검색 카탈로그`로 조정했다.
- 첫 화면은 장비 검색, 분류 필터, 지도, 선택 장비 요약, 상세 정보로 구성한다.
- 상단 메뉴는 `장비 검색 / 전체 장비 / 전장 인사이트 / 출처`로 단순화했다.
- 3D는 현재 핵심 기능에서 제외하고, 추후 GLB 파일을 연결할 수 있는 준비 슬롯과 공개 부품 스펙 목록으로 축소했다.
- `Compare`, `Technologies`, `Cases`, `Development`의 기존 독립 메뉴 역할은 검색 중심 흐름에 맞춰 흡수했다.
- 기존 데이터는 삭제하지 않고 장비 상세, 전장 인사이트, 출처 인덱스에서 계속 활용한다.

## 상태

completed

## 프로젝트 목적

해외 차륜형장갑차, 전차, 궤도형 장갑차, 자주포, 방공 플랫폼의 공개 출처 기반 동향을 한국어 중심 웹앱으로 제공한다. 장비별 제원, 계열차량, 전장 사례, 현대 전장 기술 연계, 3D 플레이스홀더 뷰어, 지도 기반 사례 탐색을 유지보수 가능한 정적 웹앱으로 관리한다.

## 기술 스택

- Vite
- React
- TypeScript
- Leaflet / React Leaflet
- Zod
- Vitest
- Playwright
- ESLint
- GitHub Pages

## 구현 완료 기능

- 공개 출처 기반 장비 데이터 JSON 관리
- 장비 상세, 비교, 기술, 사례 페이지
- 세계지도 기반 전장 사례 표시
- 한국어 지도 보조 라벨
- 추후 GLB 연동을 위한 모델 준비 슬롯 및 공개 부품 스펙 목록
- 차량 계열/파생형 데이터
- 현대 전장 기술 패널
- 출처 URL 검증 스크립트
- 이미지 URL 검증 스크립트
- Playwright 렌더링 검증
- GitHub Pages 배포 구조
- Harness-based development 운영 문서 체계

## 진행 중 기능

- 없음

## 위험 요소

- 일부 외부 이미지/출처는 원 사이트 정책에 따라 403, 429, 리다이렉트가 발생할 수 있다.
- 실제 GLB 모델은 아직 플레이스홀더 구조이며 Meshy.ai 결과물 교체가 필요하다.
- `README.md`, `CHANGELOG.md`의 과거 한글 일부가 콘솔에서 깨져 보일 수 있다.
- GitHub Actions 자동 배포는 토큰 workflow 권한 문제로 현재 사용하지 않고 `gh-pages` 브랜치 배포 방식을 사용한다.
- 미추적 파일 `ax-development-journey.html`은 사용자 작업물 가능성이 있어 이번 작업 범위에서 제외했다.

## 다음 우선순위

1. 실제 GLB 모델 경로별 교체 절차 정리
2. 데이터 출처 정기 재검증 주기 수립
3. GitHub Pages 배포 자동화 재검토
4. `README.md`, `CHANGELOG.md` 인코딩/문서 품질 정리
5. 장비/기술 데이터 확장 시 TASKS/EVIDENCE 동시 갱신
## 2026-06-19 상태 갱신: 3D 준비 영역 제거 및 공개 장치 스펙 패널 전환

- 현재 화면에서 `3D 모델 준비 영역`, GLB 경로 안내, Meshy.ai 준비 문구를 제거했다.
- 장비 상세와 검색 상세 영역은 `장치/부품 공개 스펙` 패널로 전환했다.
- 실제 GLB 모델 연결을 위한 `modelPath`, `hotspotPosition` 데이터 필드는 장기 호환성을 위해 유지한다.
- 검증 기준도 `.model-slot`이 아니라 `.component-spec-panel`을 확인하도록 갱신했다.
- 현재 우선순위는 3D 구현이 아니라 팀원이 장비를 검색하고 공개 장치 스펙과 출처를 빠르게 확인하는 것이다.
## 2026-06-19 상태 갱신: 검색 결과 출처 신뢰도 즉시 표시

- 장비 검색 결과 행에 출처 신뢰도 등급/점수, 공개 출처 수, 최근 확인일을 표시한다.
- 선택 장비 요약 패널에도 출처 신뢰도, 최근 확인일, 공개 출처 수를 추가했다.
- 결과 요약 복사와 CSV 다운로드에 출처 등급, 최근 확인일, 출처 수를 포함해 팀 공유 자료의 검증 맥락을 강화했다.
- 렌더/디자인 검증은 모든 장비 행에 신뢰도 배지와 확인일 라인이 표시되는지 확인한다.
## 2026-06-19 상태 갱신: 출처 신뢰도 필터 추가

- 장비 검색 필터에 `출처 신뢰도`를 추가했다.
- `High`, `Medium`, `Low` 등급으로 검색 결과를 좁힐 수 있어 검증이 필요한 장비를 빠르게 식별할 수 있다.
- 신뢰도 필터는 URL query string의 `confidence` 파라미터와 동기화되어 검색 링크 공유와 새로고침 복원이 가능하다.
- E2E/디자인 검증은 필터 5개 렌더링, Low 신뢰도 필터 결과 축소, URL 동기화를 확인한다.
## 2026-06-19 상태 갱신: 전장 사례 유무 필터 추가

- 장비 검색 필터에 `전장 사례`를 추가했다.
- `사례 있음`, `사례 없음`으로 검색 결과를 좁힐 수 있어 실제 운용 사례가 확인된 장비와 데이터 보강 대상 장비를 분리할 수 있다.
- 전장 사례 필터는 URL query string의 `cases` 파라미터와 동기화되어 검색 링크 공유와 새로고침 복원이 가능하다.
- 결과 요약 복사와 CSV 다운로드에 전장 사례 수를 포함해 공유 자료의 활용성을 높였다.
## 2026-06-19 상태 갱신: 검색 결과 정렬 기준 추가

- 장비 검색에 `정렬 기준` 선택 상자를 추가했다.
- 기본순, 출처 신뢰도 높은순, 전장 사례 많은순, 계열 많은순, 최근 확인일순으로 결과를 정렬할 수 있다.
- 정렬 기준은 URL query string의 `sort` 파라미터와 동기화되어 검색 링크 공유와 새로고침 복원이 가능하다.
- 결과 요약 복사와 CSV 다운로드는 현재 정렬된 결과 순서를 그대로 사용한다.
