# 변경사항 요약

## 2026-06-20 필터 배지 개별 해제

- 적용 중인 필터 배지를 클릭 가능한 해제 버튼으로 변경했다.
- 프리셋/공유 링크로 적용된 조건을 하나씩 제거할 수 있다.
- 배지 클릭 시 장비 목록과 URL query가 함께 갱신된다.
- 렌더/디자인 검증에 배지 클릭 후 조건 해제, 전체 목록 복원, URL 파라미터 제거 검사를 추가했다.
- `npm run quality` 통과를 확인했다.

---

## 2026-06-20 적용 필터 배지 표시

- 장비 검색 패널의 활성 필터 영역에 실제 적용 조건명을 배지로 표시했다.
- 계열, 분류, 임무, 국가, 상태, 계열 성숙도, 출처 신뢰도, 전장 사례, 데이터 상태, 정렬, 검색어를 라벨로 보여준다.
- 프리셋이나 공유 링크로 진입했을 때 현재 목록의 의미를 즉시 확인할 수 있다.
- 렌더/디자인 검증에 프리셋 적용 후 필터 배지 표시와 컨트롤 크기 검사를 추가했다.
- `npm run quality` 통과를 확인했다.

---

## 2026-06-20 선택 장비 요약 복사 추가

- 선택 장비 패널에 `요약 복사` 버튼을 추가했다.
- 복사 텍스트에는 장비명, 분류, 국가, 제조사, 운용 상태, 임무, 계열/전장 사례/연계 기술 수, 출처 신뢰도, 데이터 상태, 최근 확인일, 요약문을 포함한다.
- 선택 장비 패널 작업을 `요약 복사`, `상세 페이지 열기` 2개로 정리했다.
- 렌더/디자인 검증에 선택 장비 빠른 작업 버튼 수와 컨트롤 크기 검사를 추가했다.
- `npm run quality` 통과를 확인했다.

---

## 2026-06-20 중복 상단 메뉴 축소

- 상단 메뉴에서 `전체 장비`를 제거해 팀원이 보는 기본 메뉴를 3개로 단순화했다.
- `/equipment`와 `/equipment/:id` 라우트는 기존 링크 호환을 위해 유지한다.
- 장비 상세 및 `/equipment` 경로에서도 `장비 검색` 메뉴가 활성화되도록 정리했다.
- 렌더/디자인 검증에 메뉴 3개와 `전체 장비` 미노출 조건을 추가했다.
- `npm run quality` 통과를 확인했다.

---

## 2026-06-20 팀 작업 큐 추가

- 첫 화면 개요 영역에 `보강 필요 장비`, `출처 재확인`, `실전 사례 장비`, `계열 비교 후보` 작업 큐를 추가했다.
- 장비 관련 큐는 기존 검색 프리셋을 재사용해 클릭 즉시 필터/정렬 상태가 적용된다.
- 출처 재확인 큐는 `/sources?freshness=stale`로 이동하며 출처 페이지가 query를 초기 필터로 읽는다.
- 현재 재확인 필요 출처가 0건일 때도 빈 상태 안내를 정상 표시하도록 검증을 추가했다.
- `npm run quality` 통과를 확인했다.

---

## 2026-06-19 팀용 장비 검색 프리셋 추가

- 장비 검색 패널에 `보강 필요`, `실전 사례`, `고신뢰 출처`, `계열 많은 장비` 프리셋을 추가했다.
- 프리셋은 기존 검색 필터와 정렬 상태를 재사용하며 URL 공유 상태에도 반영된다.
- 모바일에서는 프리셋 버튼이 한 열로 정렬되어 검색 패널이 넘치지 않도록 했다.
- 렌더/디자인 검증에 프리셋 버튼 수, 결과 축소, URL 동기화, 터치 크기 검사를 추가했다.
- `npm run quality` 통과를 확인했다.

---

## 2026-06-19 불필요한 3D/구형 코드 제거

- 현재 검색 중심 버전에서 사용하지 않는 3D 런타임 의존성을 제거했다.
- 제거한 패키지: `@react-three/drei`, `@react-three/fiber`, `three`, `pngjs`
- 미사용 `DevelopmentLensPage`, `TrendPanel` 컴포넌트를 삭제했다.
- 3D 캔버스 조작 UI, 핫스팟 버튼, 삭제된 개발 렌즈 페이지 전용 CSS를 제거했다.
- CSS 산출물 크기를 44.47 kB에서 39.47 kB로 줄였다.
- `npm run quality` 통과를 확인했다.

---

## 2026-06-19 검색 결과 내보내기 추가

- 필터된 장비 목록을 요약 텍스트로 복사하는 버튼을 추가했다.
- 필터된 장비 목록을 CSV로 다운로드하는 버튼을 추가했다.
- CSV에는 장비명, 분류, 국가, 원산국, 제조사, 운용 상태, 임무 태그, 계열 수, 출처 신뢰도, 상세 ID를 포함한다.
- 내보내기 버튼이 모바일에서 세로로 정렬되도록 반응형 스타일을 추가했다.
- E2E/디자인 검증에 결과 내보내기 버튼과 CSV 다운로드 파일명 검증을 추가했다.
- `npm run quality` 통과를 확인했다.

---

## 2026-06-19 검색 조건 공유 링크 추가

- 장비 검색 조건을 URL query string과 동기화했다.
- 검색어, 차륜/궤도 계열, 장비 분류, 임무, 국가, 운용 상태, 계열 성숙도, 선택 장비가 링크로 복원된다.
- 검색 패널에 `검색 링크 복사` 버튼을 추가했다.
- 새로고침 후에도 필터 결과가 유지되도록 개선했다.
- E2E/디자인 검증에 URL 동기화와 reload 복원 검증을 추가했다.
- `npm run quality` 통과를 확인했다.

---

## 2026-06-19 팀원용 장비 검색 필터 고도화

- 장비 검색 패널에 `임무/파생형`, `국가`, `운용 상태`, `계열 성숙도` 필터를 추가했다.
- 자유 검색 대상에 계열차량 명칭, 임무, 무장, 성숙도를 포함했다.
- 적용 중인 검색 조건 수와 초기화 버튼을 추가했다.
- 검색 결과 행에 역할 태그와 계열 수를 표시하도록 개선했다.
- 렌더링/디자인 검증 스크립트가 새 필터 UI와 축소 동작을 확인하도록 갱신했다.
- `npm run quality` 통과를 확인했다.

---

## 1.0 분석형 사이트 개편

- 홈을 `Global Land Platform Intelligence` 대시보드로 개편했습니다.
- 라우팅 페이지를 추가했습니다.
  - `/`
  - `/equipment`
  - `/equipment/:id`
  - `/compare`
  - `/technologies`
  - `/cases`
- 장비 데이터 스키마에 분석형 필드를 추가했습니다.
  - 운용국, 플랫폼 계열, 임무 태그, 파생형 연결, 기술 태그, 사례 연결
  - 현대화 확장성, 드론 위협 압력, 체계 통합도, 야전 개조 민감도, 출처 신뢰도
- 현대 전장 사례 데이터셋 `public/data/battlefieldCases.json`을 추가했습니다.
- 비교 분석 페이지를 추가했습니다.
  - 기본 제원뿐 아니라 플랫폼 계열성, 기술 연계, 전장 적응성, 출처 신뢰도를 비교합니다.
- 기술 페이지와 사례 페이지를 추가했습니다.
- 3D 뷰어를 개선했습니다.
  - 라벨 토글
  - 기본형/개량형 비교 모드
  - 전장 적응 패키지 프리셋
  - RCWS, 30mm, APS, EW, Drone Cage 개념 오버레이
- 이미지 검증을 정리했습니다.
  - 실제 404 이미지는 제거했습니다.
  - Wikimedia 요청 제한 429는 실패가 아닌 일시 제한으로 분류합니다.
- 정적 서버 `serve:dist`가 SPA 라우트 fallback을 지원하도록 수정했습니다.

## 검증

통과한 명령:

```powershell
& "C:\Program Files\nodejs\npm.cmd" run quality
& "C:\Program Files\nodejs\npm.cmd" run images:check
```

확인된 범위:

- 장비 15개
- 파생형 30개
- 장치 스펙 23개
- 현대 전장 기술 7개
- 현대 전장 사례 7개
- 지도 마커 12개
- `/`, `/equipment`, `/equipment/:id`, `/compare`, `/technologies`, `/cases` 렌더링 확인
- 모바일 가로 오버플로 없음
- 실제 이미지 404 실패 0개
# 변경 이력

## 2026-06-19 운영 문서 체계 도입

- `AGENTS.md`를 추가해 harness-based development loop를 프로젝트 기본 운영 방식으로 명시했다.
- `PROJECT_STATE.md`를 추가해 목적, 기술 스택, 완료 기능, 위험 요소, 다음 우선순위를 기록했다.
- `TASKS.md`를 추가해 상태 머신 기반 작업 목록을 기록하도록 했다.
- `DECISIONS.md`를 추가해 주요 의사결정과 근거를 기록하도록 했다.
- `EVIDENCE.md`를 추가해 검증 명령, 결과, 리스크를 기록하도록 했다.
- 기존 앱 코드와 데이터 구조는 변경하지 않았다.

---
## 2026-06-19 장비 검색 카탈로그 중심 UI 개편

- 첫 화면을 브리핑형 대시보드에서 장비 검색 카탈로그로 재구성했다.
- 상단 메뉴를 `장비 검색 / 전체 장비 / 전장 인사이트 / 출처`로 단순화했다.
- `Compare` 별도 메뉴를 제거하고 기존 `/compare` 경로는 검색 화면으로 흡수했다.
- `Technologies`, `Cases`, `Development` 별도 흐름을 `전장 인사이트`로 통합했다.
- 기본 3D 캔버스를 제거하고 추후 GLB 연동 슬롯과 부품 스펙 목록으로 단순화했다.
- 검색 카탈로그 목적에 맞게 Playwright, 디자인, 개발 서버 검증 기준을 갱신했다.
- `npm run quality` 통과를 확인했다.

---
## 2026-06-19 3D 준비 영역 제거 및 공개 장치 스펙 패널 전환

- 화면에 남아 있던 3D/GLB 준비 영역을 제거했다.
- 장비 상세 보조 영역을 `장치/부품 공개 스펙` 패널로 전환했다.
- 장치 버튼 클릭 시 기존처럼 상세 스펙 팝업을 열 수 있게 유지했다.
- Battlefield Lens와 이미지 대체 문구에서 3D 전제 표현을 제거했다.
- Playwright/디자인/개발 서버 검증 기준을 `.component-spec-panel`로 갱신했다.
- `npm run quality` 통과를 확인했다.

---
## 2026-06-19 검색 결과 출처 신뢰도 즉시 표시

- 장비 검색 결과 행에 출처 신뢰도 배지, 공개 출처 수, 최근 확인일을 추가했다.
- 선택 장비 요약 패널에 출처 신뢰도, 최근 확인일, 공개 출처 수를 추가했다.
- 결과 요약 복사와 CSV 다운로드에 출처 등급, 최근 확인일, 출처 수를 포함했다.
- E2E/디자인 검증에서 모든 검색 행의 신뢰도 배지와 확인일 라인을 확인하도록 했다.
- `npm run quality` 통과를 확인했다.

---
## 2026-06-19 출처 신뢰도 필터 추가

- 장비 검색 패널에 `출처 신뢰도` 필터를 추가했다.
- `High`, `Medium`, `Low` 등급별로 검색 결과를 좁힐 수 있게 했다.
- 검색 링크 공유와 새로고침 복원을 위해 `confidence` URL 파라미터를 추가했다.
- E2E/디자인 검증에서 필터 5개, Low 신뢰도 결과 축소, URL 동기화를 확인하도록 했다.
- `npm run quality` 통과를 확인했다.

---
## 2026-06-19 전장 사례 유무 필터 추가

- 장비 검색 패널에 `전장 사례` 필터를 추가했다.
- `사례 있음`, `사례 없음` 기준으로 검색 결과를 좁힐 수 있게 했다.
- 검색 링크 공유와 새로고침 복원을 위해 `cases` URL 파라미터를 추가했다.
- 결과 요약 복사와 CSV 다운로드에 전장 사례 수를 포함했다.
- E2E/디자인 검증에서 필터 6개, 사례 있음 결과 축소, URL 동기화를 확인하도록 했다.
- `npm run quality` 통과를 확인했다.

---
## 2026-06-19 검색 결과 정렬 기준 추가

- 장비 검색 패널에 `정렬 기준` 선택 상자를 추가했다.
- 기본순, 출처 신뢰도 높은순, 전장 사례 많은순, 계열 많은순, 최근 확인일순을 지원한다.
- 검색 링크 공유와 새로고침 복원을 위해 `sort` URL 파라미터를 추가했다.
- 결과 요약 복사와 CSV 다운로드가 현재 정렬된 순서를 그대로 사용하도록 했다.
- E2E/디자인 검증에서 select 7개, 전장 사례 많은순 첫 결과, URL 동기화를 확인하도록 했다.
- `npm run quality` 통과를 확인했다.

---

## 2026-06-19 검색 결과 핵심 지표 스트립

- 장비 검색 결과 행에 `운용국`, `계열`, `전장 사례`, `출처` 4개 핵심 지표를 추가했다.
- 목록 단계에서 장비별 운용 확산도, 계열 규모, 실전 사례 여부, 공개 출처 규모를 바로 비교할 수 있게 했다.
- 렌더/디자인 검증에 지표 스트립 존재 여부와 지표 셀 개수 검증을 추가했다.

## 2026-06-19 데이터 보강 필요 필터

- 장비 검색 필터에 `데이터 상태`를 추가했다.
- `검증 양호`와 `보강 필요`로 장비를 필터링할 수 있게 했다.
- 보강 필요 기준은 출처 신뢰도 68 미만, 공개 출처 2건 미만, 계열 데이터 없음이다.
- 장비 결과 행에 데이터 상태 배지를 표시하고, 보강 사유를 출처 확인 줄에 함께 표시했다.
- 결과 요약 복사와 CSV 내보내기에 데이터 상태와 보강 사유를 포함했다.

## 2026-06-19 출처 인덱스 필터/확인 상태

- `/sources` 화면에 출처 검색, 출처 유형 필터, 확인 상태 필터를 추가했다.
- `checkedAt` 기준 180일 초과 출처를 `재확인 필요`로 분류하도록 했다.
- 최근 확인, 재확인 필요, 현재 표시 건수를 출처 인덱스 상단에 표시했다.
- 출처 카드에 확인 상태를 함께 표시했다.
- 렌더/디자인 검증에 출처 필터 동작 검증을 추가했다.

## 2026-06-19 출처 필터 결과 내보내기

- `/sources` 화면에 `출처 요약 복사`와 `출처 CSV 다운로드`를 추가했다.
- 현재 검색어, 출처 유형, 확인 상태 필터가 적용된 결과만 복사/다운로드하도록 했다.
- 출처 CSV 파일명을 `source-index-results.csv`로 고정했다.
- 렌더/디자인 검증에 출처 내보내기 버튼과 CSV 다운로드 파일명 검증을 추가했다.
